spring:
  application:
    name: user-service
  cloud:
    kubernetes:
      enabled: false
server:
  port: 8080
  forward-headers-strategy: framework
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}